{
  "id": "gate-build",
  "title": "TypeScript build gate",
  "description": "Build the published dist artifacts before release verification.",
  "input": "Run `npm run build` from the repo root and require the dist output to compile successfully.",
  "command": "bash -lc 'REPO_ROOT=${PROJECT_DIR:-$(pwd)}; cd \"$REPO_ROOT\"; npm run build'",
  "artifacts": [
    "dist/bin/orca.js",
    "dist/index.js"
  ],
  "graders": [
    "exit_code_ok",
    "gate_build_writes_dist_artifacts"
  ]
}
