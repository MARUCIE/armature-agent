{
  "id": "fast-chat-image-no-baseurl",
  "title": "Chat --image keeps the prompt text and fails on missing baseURL",
  "description": "Black-box smoke for `armature chat --image` when the selected provider has no baseURL configured.",
  "input": "Create a custom provider with no `baseURL`, create a real image file, run `armature chat --image <file> \"describe the issue\"`, and verify the CLI rejects the provider boundary instead of mis-parsing the prompt as another image path.",
  "command": "bash -lc 'REPO_ROOT=${PROJECT_DIR:-$(pwd)}; cd \"$REPO_ROOT\"; bash agent-eval/scripts/prepare-fast-gate.sh >/dev/null; TMP_ROOT=$(mktemp -d); trap \"rm -rf $TMP_ROOT\" EXIT; mkdir -p \"$TMP_ROOT/home\"; printf \"\\x89PNG\" > \"$TMP_ROOT/shot.png\"; cat > \"$TMP_ROOT/.armature.json\" <<EOF\n{\n  \"providers\": {\n    \"dummy\": {\n      \"apiKey\": \"test-dummy-key\",\n      \"defaultModel\": \"dummy-model\"\n    }\n  },\n  \"defaultProvider\": \"dummy\"\n}\nEOF\nOUTPUT=$(cd \"$TMP_ROOT\" && env -i PATH=\"$PATH\" HOME=\"$TMP_ROOT/home\" ARMATURE_HOME=\"$TMP_ROOT/home/.armature\" node \"$REPO_ROOT/dist/bin/armature.js\" chat --image \"$TMP_ROOT/shot.png\" \"describe the issue\" 2>&1; STATUS=$?; printf \"status=%s\\n%s\\n\" \"$STATUS\" \"$OUTPUT\"); printf \"%s\\n\" \"$OUTPUT\"; printf \"%s\" \"$OUTPUT\" | grep -q \"status=1\"; ! printf \"%s\" \"$OUTPUT\" | grep -q \"Image not found:\"; printf \"chat-image-ok\\n\"'",
  "artifacts": [],
  "graders": [
    "exit_code_ok",
    "chat_image_no_baseurl_has_provider_message",
    "chat_image_no_baseurl_has_marker"
  ]
}
