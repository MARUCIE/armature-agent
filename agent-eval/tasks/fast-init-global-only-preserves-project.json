{
  "id": "fast-init-global-only-preserves-project",
  "title": "Init --global-only preserves an existing project config",
  "description": "Black-box smoke for `orca init --global-only` when the project config already exists.",
  "input": "Seed an existing project `.orca.json`, run `orca init --global-only`, and verify the project config content remains unchanged while the global config still exists.",
  "command": "bash -lc 'REPO_ROOT=${PROJECT_DIR:-$(pwd)}; cd \"$REPO_ROOT\"; bash agent-eval/scripts/prepare-fast-gate.sh >/dev/null; TMP_HOME=$(mktemp -d); TMP_PROJECT=$(mktemp -d); trap \"rm -rf $TMP_HOME $TMP_PROJECT\" EXIT; mkdir -p \"$TMP_HOME/.orca\"; cat > \"$TMP_PROJECT/.orca.json\" <<EOF\n{\"providers\":{\"poe\":{\"apiKey\":\"keep-project\"}},\"defaultProvider\":\"poe\"}\nEOF\nOUTPUT=$(cd \"$TMP_PROJECT\" && env -i PATH=\"$PATH\" HOME=\"$TMP_HOME\" ORCA_HOME=\"$TMP_HOME/.orca\" node \"$REPO_ROOT/dist/bin/orca.js\" init --global-only); printf \"%s\\n\" \"$OUTPUT\"; test -f \"$TMP_HOME/.orca/config.json\"; grep -q \"keep-project\" \"$TMP_PROJECT/.orca.json\"; printf \"init-global-only-preserve-ok\\n\"'",
  "artifacts": [],
  "graders": [
    "exit_code_ok",
    "init_default_has_global_config_line",
    "init_default_has_next_steps",
    "init_global_only_preserve_has_marker"
  ]
}
