{
  "id": "fast-providers-test-no-baseurl",
  "title": "Providers test reports missing baseURL clearly",
  "description": "Black-box smoke for `armature providers test <id>` when the selected custom provider has no baseURL configured.",
  "input": "Create a custom provider config without `baseURL`, run `armature providers test dummy`, and verify the command reports `FAIL — no baseURL configured` with exit code 1.",
  "command": "bash -lc 'REPO_ROOT=${PROJECT_DIR:-$(pwd)}; cd \"$REPO_ROOT\"; bash agent-eval/scripts/prepare-fast-gate.sh >/dev/null; TMP_ROOT=$(mktemp -d); trap \"rm -rf $TMP_ROOT\" EXIT; mkdir -p \"$TMP_ROOT/home\"; cat > \"$TMP_ROOT/.armature.json\" <<EOF\n{\n  \"providers\": {\n    \"dummy\": {\n      \"apiKey\": \"test-dummy-key\",\n      \"defaultModel\": \"dummy-model\"\n    }\n  },\n  \"defaultProvider\": \"dummy\"\n}\nEOF\nOUTPUT=$(cd \"$TMP_ROOT\" && env -i PATH=\"$PATH\" HOME=\"$TMP_ROOT/home\" ARMATURE_HOME=\"$TMP_ROOT/home/.armature\" node \"$REPO_ROOT/dist/bin/armature.js\" providers test dummy 2>&1; STATUS=$?; printf \"status=%s\\n%s\\n\" \"$STATUS\" \"$OUTPUT\"); printf \"%s\\n\" \"$OUTPUT\"; printf \"%s\" \"$OUTPUT\" | grep -q \"status=1\"'",
  "artifacts": [],
  "graders": [
    "exit_code_ok",
    "providers_test_no_baseurl_has_testing_local",
    "providers_test_no_baseurl_has_fail",
    "providers_test_no_baseurl_has_message"
  ]
}
