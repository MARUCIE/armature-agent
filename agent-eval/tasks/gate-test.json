{
  "id": "gate-test",
  "title": "Vitest deterministic suite gate",
  "description": "Run the full deterministic repository test suite.",
  "input": "Run `npm test` from the repo root and require the full suite to pass.",
  "command": "bash -lc 'REPO_ROOT=${PROJECT_DIR:-$(pwd)}; cd \"$REPO_ROOT\"; npm test'",
  "artifacts": [],
  "graders": [
    "exit_code_ok"
  ]
}
