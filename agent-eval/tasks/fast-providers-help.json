{
  "id": "fast-providers-help",
  "title": "Providers help exposes test subcommand",
  "description": "Command-surface smoke for `armature providers --help`.",
  "input": "Run `armature providers --help` and verify the test subcommand is still advertised.",
  "command": "bash -lc 'REPO_ROOT=${PROJECT_DIR:-$(pwd)}; cd \"$REPO_ROOT\"; bash agent-eval/scripts/prepare-fast-gate.sh >/dev/null; node dist/bin/armature.js providers --help'",
  "artifacts": [],
  "graders": [
    "exit_code_ok",
    "providers_help_has_test_subcommand"
  ]
}
