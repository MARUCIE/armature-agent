{
  "id": "fast-init-preserves-existing",
  "title": "Init preserves existing global and project config files",
  "description": "Black-box smoke for `armature init` when both target config files already exist.",
  "input": "Seed both global and project config files with sentinel values, run `armature init`, and verify the command does not overwrite the existing configs.",
  "command": "bash -lc 'REPO_ROOT=${PROJECT_DIR:-$(pwd)}; cd \"$REPO_ROOT\"; bash agent-eval/scripts/prepare-fast-gate.sh >/dev/null; TMP_HOME=$(mktemp -d); TMP_PROJECT=$(mktemp -d); trap \"rm -rf $TMP_HOME $TMP_PROJECT\" EXIT; mkdir -p \"$TMP_HOME/.armature\"; cat > \"$TMP_HOME/.armature/config.json\" <<EOF\n{\"providers\":{\"openai\":{\"apiKey\":\"keep-me\"}},\"defaultProvider\":\"openai\"}\nEOF\ncat > \"$TMP_PROJECT/.armature.json\" <<EOF\n{\"providers\":{\"poe\":{\"apiKey\":\"keep-project\"}},\"defaultProvider\":\"poe\"}\nEOF\nOUTPUT=$(cd \"$TMP_PROJECT\" && env -i PATH=\"$PATH\" HOME=\"$TMP_HOME\" ARMATURE_HOME=\"$TMP_HOME/.armature\" node \"$REPO_ROOT/dist/bin/armature.js\" init); printf \"%s\\n\" \"$OUTPUT\"; grep -q \"keep-me\" \"$TMP_HOME/.armature/config.json\"; grep -q \"keep-project\" \"$TMP_PROJECT/.armature.json\"; printf \"init-preserve-ok\\n\"'","artifacts":[],"graders":["exit_code_ok","init_default_has_global_config_line","init_default_has_project_config_line","init_preserve_has_marker"]}
