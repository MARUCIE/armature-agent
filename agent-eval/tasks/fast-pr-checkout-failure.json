{
  "id": "fast-pr-checkout-failure",
  "title": "PR command reports checkout failures clearly",
  "description": "Black-box smoke for `orca pr` when `gh pr checkout` fails after metadata fetch succeeds.",
  "input": "Stub `gh` so version, view, and diff succeed but `gh pr checkout` fails, run `orca pr 123`, and verify the command reports a checkout failure for that PR number.",
  "command": "bash -lc 'REPO_ROOT=${PROJECT_DIR:-$(pwd)}; cd \"$REPO_ROOT\"; bash agent-eval/scripts/prepare-fast-gate.sh >/dev/null; TMP_ROOT=$(mktemp -d); trap \"rm -rf $TMP_ROOT\" EXIT; mkdir -p \"$TMP_ROOT/bin\" \"$TMP_ROOT/home\"; cat > \"$TMP_ROOT/bin/gh\" <<'\"'\"'EOF'\"'\"'\n#!/usr/bin/env bash\nif [[ \"$1\" == \"--version\" ]]; then echo \"gh version 9.9.9\"; exit 0; fi\nif [[ \"$1\" == \"pr\" && \"$2\" == \"view\" ]]; then\n  if [[ \"${7:-}\" == \".title\" ]]; then echo \"Demo PR\"; exit 0; fi\n  if [[ \"${7:-}\" == \".body\" ]]; then echo \"Demo body\"; exit 0; fi\nfi\nif [[ \"$1\" == \"pr\" && \"$2\" == \"diff\" ]]; then echo \"diff --git a/a b/a\"; exit 0; fi\nif [[ \"$1\" == \"pr\" && \"$2\" == \"checkout\" ]]; then echo \"checkout failed\" >&2; exit 1; fi\nexit 1\nEOF\nchmod +x \"$TMP_ROOT/bin/gh\"; OUTPUT=$(env -i PATH=\"$TMP_ROOT/bin:$PATH\" HOME=\"$TMP_ROOT/home\" node dist/bin/orca.js pr 123 2>&1; STATUS=$?; printf \"status=%s\\n%s\\n\" \"$STATUS\" \"$OUTPUT\"); printf \"%s\\n\" \"$OUTPUT\"; printf \"%s\" \"$OUTPUT\" | grep -q \"status=1\"'",
  "artifacts": [],
  "graders": [
    "exit_code_ok",
    "pr_failure_has_fetching_banner",
    "pr_checkout_failure_has_checkout_banner",
    "pr_checkout_failure_has_error_heading",
    "pr_checkout_failure_has_checkout_text"
  ]
}
