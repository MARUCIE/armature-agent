{
  "id": "fast-session-delete-missing",
  "title": "Session delete reports missing IDs clearly",
  "description": "Black-box smoke for `armature session delete <id>` when no matching saved session exists.",
  "input": "Run `armature session delete missing` in an isolated empty ARMATURE_HOME and verify the command reports the missing session id with exit code 1.",
  "command": "bash -lc 'REPO_ROOT=${PROJECT_DIR:-$(pwd)}; cd \"$REPO_ROOT\"; bash agent-eval/scripts/prepare-fast-gate.sh >/dev/null; TMP_ROOT=$(mktemp -d); trap \"rm -rf $TMP_ROOT\" EXIT; mkdir -p \"$TMP_ROOT/home\"; OUTPUT=$(env -i PATH=\"$PATH\" HOME=\"$TMP_ROOT/home\" ARMATURE_HOME=\"$TMP_ROOT/home/.armature\" node dist/bin/armature.js session delete missing 2>&1; STATUS=$?; printf \"status=%s\\n%s\\n\" \"$STATUS\" \"$OUTPUT\"); printf \"%s\\n\" \"$OUTPUT\"; printf \"%s\" \"$OUTPUT\" | grep -q \"status=1\"'",
  "artifacts": [],
  "graders": [
    "exit_code_ok",
    "session_missing_has_error_heading",
    "session_missing_has_missing_id"
  ]
}
